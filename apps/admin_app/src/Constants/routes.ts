const LOGIN = '/log-in';
const WILDCARD = '/*';
const HOME = '/home'

// For auth users
const BALANCE = '/balance';
const TRANSFER = '/transfer';

// For admin users
const ADMIN = '/admin';
const CREATE_ASA = '/create-asa';

export {
    LOGIN,
    WILDCARD,
    HOME,
    BALANCE,
    TRANSFER,
    ADMIN,
    CREATE_ASA
}